version: 2

sources:
  - name: raw_qwt
    database: "{{env_var('DBT_SOURCEDB','QWT_ANALYTICS_DEV')}}"
    schema: "{{env_var('DBT_RAWSCHEMA', 'RAW_DEV')}}"
    tables:
      - name: customers
      - name: products
      - name: order_details
      - name: orders
      - name: Employee
      - name: office
      - name: Shipment
      - name: supplierjson
